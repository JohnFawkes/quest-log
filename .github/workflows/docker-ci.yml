name: Build, Test, and Publish

# Trigger on Pull Requests (ignoring README)
# Also triggers on Push to main to ensure 'latest' tag is updated
on:
  push:
    branches: [ "main" ]
    paths:
      - '.github/**'
      - 'Dockerfile'
      - 'app.py'
      - 'requirements.txt'
      - 'templates/**'
      - 'static/**'

  workflow_dispatch:
  
  pull_request:
    branches: [ "main" ]
    paths:
      - '.github/**'
      - 'Dockerfile'
      - 'app.py'
      - 'requirements.txt'
      - 'templates/**'
      - 'static/**'

env:
  REGISTRY: ghcr.io

jobs:
  build-test-publish:
    runs-on: ubuntu-latest
    permissions:
      contents: write
      packages: write
      id-token: write
      security-events: write

    steps:
      - name: Checkout repository
        uses: actions/checkout@v7
        with:
          fetch-depth: 0 # CHANGED: Required for version calculation history

      # Bump version and push tag (Only on push to main)
      # Semver rules driven by Conventional Commits:
      #   feat:           → minor  (0.2.0 → 0.3.0)
      #   fix: / perf:    → patch  (0.3.0 → 0.3.1)
      #   BREAKING CHANGE → major  (0.3.0 → 1.0.0)
      #   anything else   → patch  (chore, docs, ci, refactor, style)
      - name: Bump version and push tag
        id: tag_version
        if: github.event_name == 'push'
        uses: mathieudutour/github-tag-action@v6.2
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          default_bump: patch
          major_wording: "BREAKING CHANGE,major:"
          minor_wording: "feat:"
          patch_wording: "fix:,perf:"

      # NEW: Create a GitHub Release (Only on push to main)
      - name: Create a GitHub Release
        if: github.event_name == 'push'
        uses: softprops/action-gh-release@v3
        with:
          tag_name: ${{ steps.tag_version.outputs.new_tag }}
          name: Release ${{ steps.tag_version.outputs.new_tag }}
          body: ${{ steps.tag_version.outputs.changelog }}
          generate_release_notes: true

      # Fix for uppercase letters in repo names (Docker requires lowercase)
      - name: Lowercase Image Name
        run: |
          echo "IMAGE_NAME=${GITHUB_REPOSITORY,,}" >> ${GITHUB_ENV}

      - name: Install cosign
        if: github.event_name != 'pull_request'
        uses: sigstore/cosign-installer@v4.1.2
        with:
          cosign-release: 'v2.1.1'

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v4

      - name: Log in to the Container registry
        uses: docker/login-action@v4
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      # Automatically generate tags:
      # - PRs get: ghcr.io/owner/repo:pr-123
      # - Main gets: ghcr.io/owner/repo:latest
      - name: Extract metadata (tags, labels) for Docker
        id: meta
        uses: docker/metadata-action@v6
        with:
          images: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}
          tags: |
            type=raw,value=latest,enable={{is_default_branch}}
            # NEW: Add the specific version tag (e.g. v1.0.5) to the docker image
            type=raw,value=${{ steps.tag_version.outputs.new_tag }},enable=${{ github.event_name == 'push' }}
            type=ref,event=branch
            type=ref,event=pr
            type=sha

      # Build and Load into local Docker daemon for testing
      - name: Build Docker image (Load Local)
        uses: docker/build-push-action@v7
        with:
          context: .
          push: false
          load: true
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          build-args: |
            COMMIT_SHA=${{ github.sha }}
          cache-from: type=gha

      - name: Run Container for Testing
        run: |
          # Get the first tag generated by the metadata step
          TAGS="${{ steps.meta.outputs.tags }}"
          TEST_TAG=$(echo "$TAGS" | head -n 1)
          
          echo "Starting container with tag: $TEST_TAG"
          # Run disconnected (-d) mapped to port 5000
          docker run -d -p 5000:5000 --name test-container "$TEST_TAG"

      - name: Wait for container to start (with retry)
        run: |
          echo "Waiting for service to be ready..."
          # Retry loop: Try curl 10 times with 2s delay
          for i in {1..10}; do
            if curl -s -o /dev/null http://localhost:5000; then
              echo "Service is up!"
              exit 0
            fi
            echo "Waiting..."
            sleep 2
          done
          echo "Service failed to start in time."
          docker logs test-container
          exit 1

      - name: Test Homepage Availability
        run: |
          # Check URL. -L follows redirects (root -> /login).
          # -v provides verbose output for debugging
          # --fail-with-body allows us to see error pages if 500/404 happens
          
          echo "Testing homepage access..."
          HTTP_STATUS=$(curl -o /dev/null -s -w "%{http_code}\n" -L http://localhost:5000)
          echo "HTTP Status: $HTTP_STATUS"
          
          if [ "$HTTP_STATUS" -eq "200" ]; then
            echo "✅ Test Passed: Website is up and accessible."
          else
            echo "❌ Test Failed: Website returned status $HTTP_STATUS"
            echo "--- Container Logs ---"
            docker logs test-container
            exit 1
          fi

      - name: Install Playwright
        run: |
          pip install playwright
          playwright install --with-deps chromium

      - name: Take demo screenshots
        run: |
          python3 - <<'EOF'
          import asyncio
          from playwright.async_api import async_playwright
          BASE = "http://localhost:5000"

          async def screenshot_page(page, path):
              """Scroll to bottom then back to top so lazy-loaded content renders, then full-page capture."""
              await page.evaluate("window.scrollTo(0, document.body.scrollHeight)")
              await page.wait_for_timeout(400)
              await page.evaluate("window.scrollTo(0, 0)")
              await page.wait_for_timeout(200)
              await page.screenshot(path=path, full_page=True)
              print(f"Saved {path}")

          async def main():
              async with async_playwright() as p:
                  browser = await p.chromium.launch()
                  ctx = await browser.new_context(viewport={"width": 1280, "height": 900})
                  page = await ctx.new_page()

                  # Login page (unauthenticated)
                  await page.goto(f"{BASE}/login")
                  await page.wait_for_load_state("networkidle")
                  await screenshot_page(page, "screenshot-login.png")

                  # Log in as demo user
                  await page.goto(f"{BASE}/login/demo")
                  await page.wait_for_load_state("networkidle")

                  # Dashboard
                  await page.goto(f"{BASE}/dashboard")
                  await page.wait_for_load_state("networkidle")
                  await page.wait_for_timeout(1000)
                  await screenshot_page(page, "screenshot-dashboard.png")

                  # Avatar
                  await page.goto(f"{BASE}/avatar")
                  await page.wait_for_load_state("networkidle")
                  await screenshot_page(page, "screenshot-avatar.png")

                  # Avatar Shop
                  await page.goto(f"{BASE}/avatar/shop")
                  await page.wait_for_load_state("networkidle")
                  await screenshot_page(page, "screenshot-avatar-shop.png")

                  # Rewards
                  await page.goto(f"{BASE}/rewards")
                  await page.wait_for_load_state("networkidle")
                  await screenshot_page(page, "screenshot-rewards.png")

                  # Calendar
                  await page.goto(f"{BASE}/calendar")
                  await page.wait_for_load_state("networkidle")
                  await screenshot_page(page, "screenshot-calendar.png")

                  # Settings / Profile
                  await page.goto(f"{BASE}/settings/profile")
                  await page.wait_for_load_state("networkidle")
                  await screenshot_page(page, "screenshot-settings.png")

                  await browser.close()
          asyncio.run(main())
          EOF

      - name: Commit screenshots to repo
        if: github.event_name == 'push'
        run: |
          mkdir -p screenshots
          cp screenshot-login.png      screenshots/login.png
          cp screenshot-dashboard.png  screenshots/dashboard.png
          cp screenshot-avatar.png     screenshots/avatar.png
          cp screenshot-avatar-shop.png screenshots/avatar-shop.png
          cp screenshot-rewards.png    screenshots/rewards.png
          cp screenshot-calendar.png   screenshots/calendar.png
          cp screenshot-settings.png   screenshots/settings.png
          # Remove any stale screenshots no longer generated
          git rm --ignore-unmatch screenshots/invites.png screenshots/screenshot-*.png
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add screenshots/
          git diff --staged --quiet && echo "No changes to screenshots." || \
            git commit -m "chore: update demo screenshots [skip ci]" && \
            git push origin HEAD:${{ github.ref_name }}

      - name: Upload screenshots as artifact
        uses: actions/upload-artifact@v7
        if: always()
        with:
          name: demo-screenshots-${{ github.sha }}
          path: screenshot-*.png
          retention-days: 30

      - name: Stop Test Container
        if: always()
        run: docker rm -f test-container

      - name: Install Trivy
        run: |
          wget -qO - https://aquasecurity.github.io/trivy-repo/deb/public.key \
            | gpg --dearmor \
            | sudo tee /usr/share/keyrings/trivy.gpg > /dev/null
          echo "deb [signed-by=/usr/share/keyrings/trivy.gpg] https://aquasecurity.github.io/trivy-repo/deb generic main" \
            | sudo tee /etc/apt/sources.list.d/trivy.list
          sudo apt-get update -qq && sudo apt-get install -y trivy

      - name: Run Trivy vulnerability scanner
        uses: aquasecurity/trivy-action@v0.36.0
        with:
          image-ref: ${{ fromJSON(steps.meta.outputs.json).tags[0] }}
          format: sarif
          output: trivy-results.sarif
          ignore-unfixed: true
          vuln-type: os,library
          severity: CRITICAL,HIGH
          skip-setup-trivy: true

      - name: Upload Trivy scan results to GitHub Security
        uses: github/codeql-action/upload-sarif@v4
        if: always()
        with:
          sarif_file: trivy-results.sarif
          ref: ${{ github.ref }}
          sha: ${{ github.sha }}

      # If tests passed, build again (cached) and push to registry
      - name: Push Docker image
        uses: docker/build-push-action@v7
        with:
          context: .
          push: ${{ github.event_name != 'pull_request' }}
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          build-args: |
            COMMIT_SHA=${{ github.sha }}
          cache-from: type=gha
          cache-to: type=gha,mode=max
