version: 2
updates:
  # GitHub Actions — monthly updates
  # Note: pip dependencies are managed by Renovate (renovate.json)
  - package-ecosystem: "github-actions"
    directory: "/"
    schedule:
      interval: "monthly"
    commit-message:
      prefix: "chore"
      include: "scope"
